{
  "name": "babylonjsdoc",
  "version": "1.0.0",
  "description": "Website for the Babylon.js documentation",
  "dependencies": {
    "express": "^4.13.0",
    "grunt": "latest",
    "grunt-cli": "latest",
    "jade": "latest",
    "meta-marked": "^0.3.3"
  },
  "scripts": {
    "setup:bourbon": "gem install bourbon",
    "setup:neat": "gem install neat",
    "setup": "chcp 1252 && npm run setup:bourbon && npm run setup:neat && cd scss/libs && bourbon install && neat install",
    "tags": "grunt tags"
  }
}
